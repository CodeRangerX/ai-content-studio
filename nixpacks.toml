# Nixpacks 配置 - Railway 部署使用

[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm install', 'cd server && npm install']

[phases.build]
cmds = ['cd server && npm run build']

[start]
cmd = 'cd server && npm run start'

[variables]
NODE_ENV = 'production'